# CLAUDE.md — Nodability operating manual

**Read this file first, every session.** It is the primary operating manual for AI coding
agents working in this repository. Companion files (read as needed, not always):
[PROJECT_STATE.md](PROJECT_STATE.md), [TASKS.md](TASKS.md), [ARCHITECTURE.md](ARCHITECTURE.md),
[FILE_MAP.md](FILE_MAP.md), [FEATURES.md](FEATURES.md), [DATABASE.md](DATABASE.md),
[API_REFERENCE.md](API_REFERENCE.md), [UI_SYSTEM.md](UI_SYSTEM.md), [SECURITY.md](SECURITY.md),
[TESTING.md](TESTING.md), [DEPLOYMENT.md](DEPLOYMENT.md), [ROADMAP.md](ROADMAP.md),
[DECISIONS.md](DECISIONS.md), [SESSION_LOG.md](SESSION_LOG.md), [HANDOFF.md](HANDOFF.md).

This file was generated by a documentation-handoff audit on **2026-08-06**. Every fact below
is either **Verified** (read directly from code/config/live systems during the audit) or
**Inferred** (a reasonable conclusion not explicitly stated anywhere). Unlabeled statements
about file contents, commands, and configuration are Verified.

---

## ⚠️ Breaking convention: this is NOT the Next.js you know

`AGENTS.md` (also aliased as the old `CLAUDE.md`, both still present and still accurate)
contains this standing warning, carried forward here:

> This version has breaking changes — APIs, conventions, and file structure may all differ
> from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before
> writing any code. Heed deprecation notices.

The concrete instance of this that matters most: **Next.js 16 renamed `middleware.ts` to
`proxy.ts`.** This repo's request-gating file is `proxy.ts` at the project root, exporting a
`proxy()` function — not `middleware()`. If you create a `middleware.ts` file it will **not
run**. Verified via `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`.

---

## Project identity

- **Name:** Nodability
- **One-sentence description:** An AI-chat-driven personal task organizer for two named
  people (a couple), with a task board, an idea box, week/month/year calendar views, and
  light/dark theming.
- **Detailed summary:** Nodability lets a signed-in user type free-form messages into a chat
  panel ("talk to Nodo"); a cheap Claude model (Haiku) silently extracts structured tasks,
  categories, and deletions from the message, writes them to Supabase, and a capable Claude
  model (Sonnet) streams back a conversational reply grounded in the user's actual current
  task list. Tasks are also browsable/editable directly via a category-grouped board, a
  Week/Month/Year calendar, and a separate free-form "Ideas" scratchpad. Two accounts exist
  (magic-link, invite-only) and each account's data is fully isolated from the other's.
- **Target audience:** Exactly two specific real people — the developer and their partner.
  Not built for public signup or multi-tenant scale. (Verified: `shouldCreateUser: false` in
  `app/login/page.tsx`, and exactly 2 Supabase Auth users exist in production as of the audit.)
- **Main user problem solved:** Frictionless task capture — type what's on your mind in
  natural language instead of filling out a task form — plus multiple ways to review what's
  ahead (list, week, month, year).
- **Current development stage:** Actively-developed personal project in real daily use by its
  two users, not a hardened multi-tenant production SaaS. **Inferred** label:
  "post-MVP personal app" — real user data exists (19 tasks, 112 messages, 7 ideas, 3
  categories at audit time), but there are no automated tests, no CI, and `npm run lint`
  currently fails (see [Known issues](#known-issues)).
- **Production status:** Deployed and live at **https://nodability.vercel.app** (Verified:
  returns 200 on `/login`, 307→`/login` on protected routes when unauthenticated, as of the
  audit). Deploys are triggered **manually** via the Vercel CLI (`vercel --prod --yes`) —
  GitHub push does **not** auto-deploy (Verified empirically this session: a push to `main`
  did not produce a new Vercel deployment; no GitHub Actions workflow exists in this repo).
- **Repository type:** Single Next.js application, not a monorepo. Root directory `.` (Verified
  via `vercel project inspect nodability`).
- **⚠️ This repo's GitHub remote (`Gariyuuu/nodability`) is public.** This is a real
  constraint on what belongs in any file in this repo, including these documentation files:
  no real email addresses (a real one was found and redacted from `DATABASE.md`/
  `PROJECT_STATE.md` during the 2026-08-06 account-switch checkpoint — see `SESSION_LOG.md`),
  no secrets/keys (never an issue so far, but stay alert to it), and no other real
  third-party personal information. Refer to people by role ("the app owner," "their
  partner") rather than by name/email in anything that gets committed.

---

## Current status

- **Current stable state:** Deployed and working. Build succeeds (`npm run build`, exit 0).
  TypeScript type-checks clean (`npx tsc --noEmit`, exit 0). **ESLint now passes cleanly too**
  (`npm run lint`, exit 0) — all 9 original errors were fixed, see [Known issues](#known-issues).
- **Latest completed milestone:** A follow-up session fixed every tracked bug/tech-debt item
  from the documentation audit in one pass (commit `18200e7`): all 9 lint errors, `/api/chat`
  error handling, template re-application duplicating tasks, the unused `is_recall_query`
  field, unused scaffold assets, self-hosted theme background photos (was hotlinked from
  Unsplash), improved life-area-grouping discoverability, and the safe half of the `npm audit`
  findings. Before that: a large feature pass adding real per-user authentication, theming (4
  palettes × light/dark), a Week/Month/Year calendar with Academic/Personal/Work/Other
  grouping, a starter-templates page, a changelog page, and chat suggestion chips. See
  `git log` and [CHANGELOG.md](CHANGELOG.md) for the full commit-by-commit history.
- **Current active task:** None. Every tracked task (`DOC-001`, `BUG-001`, `BUG-002`,
  `TODO-001` through `TODO-007`) is complete. See [PROJECT_STATE.md](PROJECT_STATE.md) for the
  exact stopping point and [TASKS.md](TASKS.md) for what's left (only the deliberately-deferred
  `next` version bump, ISSUE-006).
- **Highest-priority next task:** None urgent. The only open item is ISSUE-006 (3 remaining
  `npm audit` advisories requiring a `next` version bump past the pinned range) — deliberately
  deferred, not a "next task to just do."
- **Features currently under construction:** None. Every feature that has UI for it also has
  working backend/DB wiring (see [FEATURES.md](FEATURES.md) for the per-feature verification
  table) — this is not a repo with half-built UI shells.

---

## Technology stack

All versions below are the **exact resolved versions** from `package-lock.json` /
`node_modules`, not the semver ranges in `package.json` (Verified via
`node_modules/<pkg>/package.json` versions at audit time).

| Category | Technology | Version |
|---|---|---|
| Language | TypeScript | 5.9.3 (strict mode) |
| Framework | Next.js (App Router, Turbopack) | 16.2.11 |
| UI library | React | 19.2.4 |
| | React DOM | 19.2.4 |
| Package manager | npm | 11.16.0 (local); lockfile is `package-lock.json` |
| Runtime (local) | Node.js | v26.3.0 (audit machine) |
| Runtime (Vercel) | Node.js | 24.x (Verified via `vercel project inspect`) |
| Styling | Tailwind CSS | 4.3.3 (CSS-first `@theme` config, no `tailwind.config.js`) |
| Database + Auth | Supabase (Postgres + Auth) | n/a (hosted) |
| DB client (server, service-role) | `@supabase/supabase-js` | 2.112.0 |
| DB client (session-aware, SSR) | `@supabase/ssr` | 0.12.4 |
| ORM | **None** — raw `supabase-js` query builder throughout |
| AI provider | `@anthropic-ai/sdk` | 0.113.0 |
| AI models used | `claude-haiku-4-5-20251001` (extraction), `claude-sonnet-5` (chat reply) | — |
| Hosting | Vercel | — |
| Auth provider | Supabase Auth (magic link / OTP, PKCE flow) | — |
| Storage provider | None used | n/a |
| Analytics | None | n/a |
| Payments | None | n/a |
| Email provider | Supabase's built-in auth email sender (rate-limited; no custom SMTP configured) | n/a |
| Testing libraries | **None installed** | n/a |
| Linting | ESLint (flat config) + `eslint-config-next` | 9.39.5 / 16.2.11 |
| Formatting | **No Prettier or formatter configured** — Inferred: relies on editor defaults | n/a |
| Build tool | Next.js/Turbopack (built in) | n/a |

Do not upgrade any of these versions as part of unrelated work — see
[DO NOT CHANGE WITHOUT REVIEW](#do-not-change-without-review).

---

## Essential commands

Single app, single working directory (`/Users/gariyuu/Projects/nodability` or wherever this
repo is cloned) — no monorepo path juggling.

| Purpose | Command | Verified |
|---|---|---|
| Install dependencies | `npm install` | Yes (used to install `@supabase/ssr` this session) |
| Dev server | `npm run dev` (Turbopack, port 3000) | Yes |
| Production build | `npm run build` | Yes — currently succeeds, exit 0 |
| Start production server (after build) | `npm run start` | Not run this session (Inferred standard Next.js behavior) |
| Lint | `npm run lint` | Yes — **currently fails, exit 1, 9 errors** (see Known issues) |
| Type-check only | `npx tsc --noEmit` | Yes — currently passes, exit 0 |
| Unit / integration / E2E tests | **No test command exists.** No test framework is installed and no test files exist anywhere in the repo. | N/A |
| DB migration | No CLI migration tool. Migrations are hand-written `.sql` files under `supabase/migrations/`, applied **manually** by pasting into the Supabase SQL Editor at `https://supabase.com/dashboard/project/hwvqnenmnrzdncwznorv/sql/new`. There is no `supabase/config.toml` and the Supabase CLI is not used. | Yes |
| DB seed | No seed script exists. | N/A |
| Generate types | No generated-types workflow exists (no `supabase gen types`). Table shapes are hand-written TypeScript interfaces in `lib/tasks.ts`, `lib/ideas.ts`, `lib/messages.ts`. | N/A |
| Reset local dev data | N/A — there is no local database; `npm run dev` talks to the same live Supabase project as production (see [Known issues](#known-issues) / [SECURITY.md](SECURITY.md) for the implications). | Yes |
| Deploy | `vercel --prod --yes` (manual; **not** triggered by `git push`) | Yes — used repeatedly this session |
| Provision a new user account | `node --env-file=.env.local scripts/create-users.mjs <email> [email...]` | Yes |

---

## Repository structure

```
app/                      Next.js App Router pages + API routes
  api/{tasks,ideas,categories,templates,chat}/route.ts   REST-ish JSON API, all except auth require login
  auth/callback/route.ts  Magic-link PKCE code exchange
  login/page.tsx          Public sign-in page (magic link only)
  page.tsx                Main board: Sidebar + TaskBoard + ChatPanel
  week/page.tsx            Calendar shell: Week/Month/Year tabs + group filter
  ideas/page.tsx           Idea box page
  templates/page.tsx       Starter-template picker page
  changelog/page.tsx       "What's new" page, reads lib/changelog.ts
  icon.tsx / apple-icon.tsx  Generated favicon (next/og ImageResponse)
  layout.tsx               Root layout: fonts, no-flash theme script, <ThemeProvider>
  globals.css              Tailwind v4 entry + all theme/palette CSS variables
components/
  Sidebar.tsx, TaskBoard.tsx, ChatPanel.tsx   Main-board UI pieces
  calendar/{WeekView,MonthView,YearView}.tsx  Calendar view components
  theme/{ThemeProvider,ThemeToggle}.tsx       Theme context + picker UI
lib/
  supabase.ts              Server-only service-role Supabase client (bypasses RLS)
  supabase/{server,browser}.ts   Session-aware Supabase clients (@supabase/ssr)
  auth.ts                  requireUserId() — the auth check every API route calls
  actions.ts               Server Action: signOutAction
  tasks.ts, ideas.ts, messages.ts   All DB reads/writes, every function takes userId first
  categorize.ts            Haiku tool-use call that extracts structured tasks from a message
  anthropic.ts             Anthropic client + model name constants
  prompts.ts               System prompts (chat persona "Nodo" + extraction instructions)
  calendar.ts               Month-grid / year-grid date math
  week.ts                   Week-day date math + taskFallsOnDay
  groups.ts                 Academic/Personal/Work/Other colors, labels, cycle order
  templates.ts               Starter-template definitions (Student/Work/Home & Life)
  changelog.ts                Hand-maintained changelog entries
  theme.ts                    Palette list + no-flash inline script string
  format.ts                   Date/time display formatting helpers
supabase/
  schema.sql                Original base schema (categories, tasks, messages)
  migrations/002..005.sql   Forward-only migrations, run manually in order
scripts/
  create-users.mjs          One-off admin script to provision the 2 allowed accounts
public/theme/                Self-hosted theme background photos (slate/ocean/sunset/forest.jpg) + SOURCES.md
proxy.ts                    Next.js 16 request gate (session refresh + auth redirect)
```

Full per-file detail, including "what imports/calls this" and edit risk, is in
[FILE_MAP.md](FILE_MAP.md).

---

## Architecture summary

See [ARCHITECTURE.md](ARCHITECTURE.md) for the full diagram and flow-by-flow breakdown. Short
version:

- **Rendering:** Every page **except `app/changelog/page.tsx`** is a Client Component that
  fetches its own data from the API routes via `fetch()` on mount. `app/changelog/page.tsx` is
  the one Server Component page — it renders `lib/changelog.ts`'s static array directly, no
  fetch involved. There is **no database-backed Server Component data fetching** anywhere and
  no `getServerSideProps`/RSC-database-fetch patterns in use — this is a classic
  SPA-over-API-routes shape, not an RSC-idiomatic Next.js app. (Verified via `head -1` on
  every `page.tsx`.)
- **Server/client boundary:** API routes (`app/api/*/route.ts`) and one Server Action
  (`lib/actions.ts:signOutAction`) are the only server-side code paths that touch the database.
- **Database access pattern:** All server-side DB access goes through a single **service-role**
  Supabase client (`lib/supabase.ts`), which bypasses Row Level Security entirely. Every
  query function in `lib/tasks.ts` / `lib/ideas.ts` / `lib/messages.ts` therefore takes a
  `userId` as its first argument and manually filters `.eq("user_id", userId)` — **this
  application-level filtering, not RLS, is the actual multi-tenant security boundary.**
- **Auth flow:** Magic link (Supabase OTP, PKCE) → `app/auth/callback/route.ts` exchanges the
  code for a session cookie → `proxy.ts` refreshes that cookie on every request and redirects
  unauthenticated page loads to `/login` → API routes independently call
  `lib/auth.ts:requireUserId()` and return 401 JSON if there's no session (this is intentional
  defense-in-depth, not redundancy to remove).
- **AI flow:** User message → `app/api/chat/route.ts` → `lib/categorize.ts` (Haiku, forced
  tool-use, extracts tasks/deletions) → writes to DB via `lib/tasks.ts` → builds a grounding
  context block from current open tasks + an "actions just taken" log → streams a Sonnet reply
  back to the client → persists both messages via `lib/messages.ts`.
- **State management:** Local `useState`/`useEffect` per component. No global state library,
  no React Context beyond `ThemeProvider`.
- **Caching:** None beyond Next.js's default static generation of purely-static routes
  (`/icon`, `/apple-icon`, `/changelog`, etc. — see the build output's route table).
- **Background/scheduled jobs:** None exist in this repo.

---

## Coding conventions

**Verified** (observed consistently across the codebase, not aspirational):

- Server-only DB query functions live in `lib/*.ts`, always take `userId: string` as the
  first parameter, and throw on Supabase error (`if (error) throw error;`) rather than
  swallowing it.
- API routes follow a consistent try/catch pattern: call `requireUserId()`, catch
  `UnauthorizedError` → `NextResponse.json({ error: "unauthorized" }, { status: 401 })`, else
  rethrow. **Exception:** `app/api/chat/route.ts` only wraps the `requireUserId()` call this
  way; the rest of its body has no error handling (see [Known issues](#known-issues)).
- Components are function components with named default exports, `"use client"` at the top
  of every interactive file.
- Styling is Tailwind utility classes using this app's **semantic color tokens** (`bg-bg`,
  `text-fg`, `bg-surface`, `border-border`, `text-muted`, `bg-accent`, `text-accent-fg`) —
  never raw Tailwind palette classes like `bg-gray-100` (those were deliberately removed
  during the theming pass; reintroducing them would break dark-mode/palette support — see
  [UI_SYSTEM.md](UI_SYSTEM.md)).
- Path alias `@/*` maps to the repo root (`tsconfig.json`), e.g. `@/lib/tasks`, `@/components/Sidebar`.
- No barrel files (`index.ts` re-exports) anywhere — every import is a direct file path.
- Dates are ISO-8601 strings (`YYYY-MM-DD`) end-to-end; times are 24-hour `HH:MM`. Formatting
  to human-readable strings happens only at render time in `lib/format.ts`.
- SQL migrations are numbered (`002_`, `003_`, ...), forward-only, with a comment at the top
  explaining what they do and how to run them. **001 does not exist** — `supabase/schema.sql`
  is the implicit "migration 001" (Inferred from numbering gap).

**Newly recommended** (not yet established, but would fit the existing style): if a test
framework is added, colocate `*.test.ts` next to the file under test rather than a separate
`__tests__` tree, to match this repo's otherwise-flat, colocated style.

---

## UI and design system

Full detail in [UI_SYSTEM.md](UI_SYSTEM.md). Key facts every session should know up front:

- **Theme tokens are defined in `app/globals.css`**, as CSS custom properties scoped by
  `[data-palette]` and `[data-theme]` attributes on `<html>`, then re-exposed as Tailwind v4
  utilities via an `@theme inline` block (`--color-bg`, `--color-fg`, etc. → `bg-bg`,
  `text-fg`, ...). There is **no `tailwind.config.js`** — Tailwind v4's CSS-first config is
  used exclusively.
- **4 palettes:** Slate, Ocean, Sunset, Forest — each with a light and dark variant (8 total
  variable blocks in `globals.css`). Palette + mode are chosen via `components/theme/ThemeToggle.tsx`
  and persisted to `localStorage` (`lib/theme.ts` constants `THEME_STORAGE_KEY` /
  `PALETTE_STORAGE_KEY`).
- **Backgrounds are real photos, self-hosted** at `public/theme/{slate,ocean,sunset,forest}.jpg`
  (one per palette, same photo for light/dark, darkened/lightened via a CSS scrim gradient
  layered on top). Originally sourced from Unsplash and hotlinked directly from
  `images.unsplash.com` — downloaded and self-hosted instead as of commit `18200e7` for
  durability (see `public/theme/SOURCES.md` for original photo IDs/license).
- **No-flash-of-wrong-theme script:** an inline `<script>` in `app/layout.tsx`'s `<head>`
  (string built in `lib/theme.ts` as `NO_FLASH_SCRIPT`) reads `localStorage` and sets
  `data-theme`/`data-palette` on `<html>` before first paint. Removing this reintroduces a
  flash-of-default-theme on every load.
- **No component library** (no shadcn/ui, no Radix, no MUI) — every UI element is hand-rolled
  with Tailwind classes.
- **Icons:** `app/icon.tsx` / `app/apple-icon.tsx` use `next/og`'s `ImageResponse` to render a
  gradient "n" mark at request time (statically cached by Next.js). No icon font or SVG icon
  library is used elsewhere; inline emoji are used for small decorative marks (🌱 for the chat
  avatar, 🎨 for the theme toggle button).
- **Responsive design:** Minimal — the main board (`app/page.tsx`) uses a fixed 3-column
  desktop layout (`Sidebar` / `TaskBoard` / `ChatPanel`, `w-96` chat panel) with **no mobile
  breakpoint handling**. The week/ideas/templates/changelog pages use `max-w-*` centered
  containers that do reflow reasonably on narrow screens, but nothing was verified on an
  actual mobile viewport. **Inferred gap, not a confirmed bug.**
- **Accessibility:** Basic `aria-label`/`title` attributes exist on icon-only buttons (delete
  buttons, theme toggle, group dots). No systematic a11y audit has been done.

---

## Environment setup

Canonical reference file: **`.env.local.example`** (already accurate and complete — verified
its 4 variable names match `.env.local` exactly). This project uses Next.js's own
`.env.local` convention rather than a generic `.env.example`; do not create a duplicate
`.env.example` file.

| Variable | Purpose | Required | Used in | Side | Format | Sensitive |
|---|---|---|---|---|---|---|
| `ANTHROPIC_API_KEY` | Auth for all Claude API calls (extraction + chat reply) | Yes | `lib/anthropic.ts` | Server only | Anthropic API key string | **Yes** |
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL | Yes | `lib/supabase.ts`, `lib/supabase/server.ts`, `lib/supabase/browser.ts`, `proxy.ts` | Both (name is `NEXT_PUBLIC_*`, exposed to the browser) | `https://<ref>.supabase.co` | No (project URL is not secret) |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon/public API key — used for the session-aware (RLS-respecting) client | Yes | `lib/supabase/server.ts`, `lib/supabase/browser.ts`, `proxy.ts` | Both | Supabase publishable key string | No (designed to be public, RLS is the real gate) |
| `SUPABASE_SERVICE_ROLE_KEY` | Full-privilege key that **bypasses RLS entirely** | Yes | `lib/supabase.ts`, `scripts/create-users.mjs` | **Server only — never import `lib/supabase.ts` from a `"use client"` file** | Supabase service-role JWT | **Yes, extremely** — full DB + Auth admin access |
| `VERCEL_OIDC_TOKEN` | Ambient var auto-injected by Vercel CLI tooling (`vercel dev`) | No — not read by any app code | N/A | N/A | N/A | N/A — not an app secret, don't add to `.env.local.example` |

Live production values for `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/
`SUPABASE_SERVICE_ROLE_KEY`/`ANTHROPIC_API_KEY` are set in the Vercel dashboard for **both
Production and Preview** environments (Verified via `vercel env ls`). No Development-scope
Vercel env vars exist — local dev relies entirely on `.env.local`.

**Never** print, log, or commit the actual values of `SUPABASE_SERVICE_ROLE_KEY` or
`ANTHROPIC_API_KEY`. `.env.local` is gitignored (Verified in `.gitignore`).

---

## Database summary

Full detail, including a Mermaid ER diagram, in [DATABASE.md](DATABASE.md). Short version:

- **Provider:** Supabase (managed Postgres), project ref `hwvqnenmnrzdncwznorv`.
- **Schema source of truth:** `supabase/schema.sql` + `supabase/migrations/002..005*.sql`,
  applied **in order, manually**, via the Supabase SQL Editor. There is no automated
  migration runner.
- **Tables:** `categories`, `tasks`, `messages`, `ideas` — all 4 have `user_id uuid not null
  references auth.users(id)`, RLS enabled, and an owner-only RLS policy
  (`user_id = auth.uid()`). RLS is **defense-in-depth only** — the app's actual server code
  always uses the service-role client, which ignores RLS.
- **Live data at audit time:** 19 rows in `tasks`, 112 in `messages`, 7 in `ideas`, 3 in
  `categories`. **This is real data belonging to real people — treat destructive operations
  accordingly.**
- **Known schema risk:** `categories` has a `unique(user_id, name)` index (case-sensitive),
  but application code looks up categories case-insensitively (`.ilike()`). A race or a
  manually-inserted row could create two categories differing only by case
  (e.g. "Chemistry" and "chemistry") that the app would then treat inconsistently.

---

## Authentication and authorization

Full detail in [SECURITY.md](SECURITY.md) and [API_REFERENCE.md](API_REFERENCE.md). Short
version:

- **Signup:** None — no public signup UI or flow exists anywhere in the codebase.
- **Login:** Magic link only (`app/login/page.tsx` → `supabase.auth.signInWithOtp({
  shouldCreateUser: false })`). Because `shouldCreateUser` is `false`, only pre-existing
  Supabase Auth users can request a link — this is the entire access-control mechanism for
  "who is allowed to use this app."
- **Account provisioning:** New accounts are created **out-of-band** by running
  `scripts/create-users.mjs <email>` locally with the service-role key. There is no in-app
  invite flow.
- **Logout:** `lib/actions.ts:signOutAction` (Server Action), wired to a form in
  `app/page.tsx`'s header.
- **Session handling:** `@supabase/ssr` cookie-based sessions; refreshed on every request by
  `proxy.ts`.
- **Roles/permissions:** None — there are exactly two account types, both identical
  ("owner of their own data"). No admin role, no shared/team data model.
- **Protected routes:** Every route except `/login` and `/auth/callback` (see
  `PUBLIC_PATHS` / the proxy `matcher` config in `proxy.ts`, which also excludes `/icon` and
  `/apple-icon` so the favicon loads for logged-out visitors).
- **Ownership checks:** Enforced in application code (`userId` filtering in `lib/tasks.ts` /
  `lib/ideas.ts` / `lib/messages.ts`), backed by RLS as a second layer.

---

## API and integrations

Full detail with request/response shapes in [API_REFERENCE.md](API_REFERENCE.md). Summary:

- **Internal API routes:** `GET/PATCH/DELETE /api/tasks`, `GET/PATCH /api/categories`,
  `GET/POST/DELETE /api/ideas`, `POST /api/templates`, `POST /api/chat`, `GET /auth/callback`.
  All require an authenticated session except `/auth/callback` itself.
- **External APIs:** Anthropic Claude API (no sandbox/test mode used — real API key, real
  billing on every call), Supabase (Postgres + Auth REST API). Theme background photos were
  originally hotlinked from Unsplash's CDN but are now self-hosted (`public/theme/`) — no
  external image dependency remains at runtime.
- **Webhooks:** None.
- **Rate limits:** None implemented by this app. Supabase's own auth-email rate limit applies
  to magic-link sends (has been hit and self-resolved during development — see
  [SESSION_LOG.md](SESSION_LOG.md)). No rate limiting exists on `/api/chat`, meaning nothing
  stops rapid repeated requests from accumulating Anthropic API cost.
- **Anthropic billing note (Verified via user Q&A + web research this session, not something
  the repo itself states):** `ANTHROPIC_API_KEY` billing is **pay-per-token API billing,
  completely separate from any Claude.ai consumer subscription (Pro/Max)**. A Claude Max
  subscription does not offset or cover this app's API usage.

---

## Testing and verification

Full detail in [TESTING.md](TESTING.md). Headline fact: **there is no automated test suite of
any kind** — no Jest/Vitest/Playwright/Cypress, no `__tests__` directories, no test script in
`package.json`. All verification during this project's development has been manual (curl
smoke tests, Playwright one-off screenshot scripts run ad hoc, not committed to the repo) plus
`npm run build` / `npx tsc --noEmit`.

---

## Deployment

Full detail in [DEPLOYMENT.md](DEPLOYMENT.md). Headline facts:

- **Host:** Vercel, project `nodability` (ID `prj_r3qYTjvfmaAKbRsy4b6P87Rr5U1r`), team
  `garywangsmes-8349's projects`.
- **Production URL:** https://nodability.vercel.app
- **Deploy command:** `vercel --prod --yes`, run manually from the repo root. **`git push`
  does not trigger a deploy** — there is no GitHub Actions workflow and the Vercel↔GitHub
  auto-deploy link is not active for this project (Verified empirically: pushing commits this
  session did not produce a new deployment until `vercel --prod --yes` was run directly).
- **Build command:** `npm run build` (Vercel default detection for Next.js).
- **Runtime:** Node.js 24.x on Vercel.
- **No `vercel.json`** exists — all settings are dashboard/CLI-configured defaults.
- **Migration order matters at deploy time:** app code that depends on a new column (e.g.
  `categories.group_name`, added this session) must not be deployed before the corresponding
  SQL migration has been run in the Supabase SQL Editor, or every request touching that table
  will fail. This has been the actual deploy procedure used throughout this project's
  development — see [DEPLOYMENT.md](DEPLOYMENT.md) for the exact checklist.

---

## Known issues

All 5 issues originally found during the 2026-08-06 documentation audit were fixed in a
follow-up session the same day (commit `18200e7`). Kept below, marked **Resolved**, so the
reasoning and file locations stay available — don't delete resolved issues, since the "why"
is often still useful context for related future work.

### ISSUE-001 — ESLint failed with 9 errors — **Resolved**
- **Affected files:** `app/ideas/page.tsx`, `app/week/page.tsx`, `components/TaskBoard.tsx`,
  `components/theme/ThemeProvider.tsx`, `components/ChatPanel.tsx`, `lib/tasks.ts`.
- **Fix applied:** `ThemeProvider.tsx` got a genuine structural fix — the localStorage-read
  effect was replaced with guarded lazy `useState` initializers (`readStoredMode`/
  `readStoredPalette`, falling back to defaults when `typeof window === "undefined"`), which
  eliminates the extra post-mount render entirely rather than just satisfying the linter. The
  3 fetch-on-mount cases (`app/ideas/page.tsx`, `app/week/page.tsx`,
  `components/TaskBoard.tsx`) each got a justified
  `// eslint-disable-next-line react-hooks/set-state-in-effect` with an inline comment — this
  is a standard, functionally-correct pattern the rule is simply stricter about than
  warranted here; a full data-fetching-library migration (SWR/React Query/Suspense) was
  judged disproportionate for this app's scale. The unescaped apostrophe in `ChatPanel.tsx`
  was fixed directly (`&apos;`). The 4 `: any` casts in `lib/tasks.ts` were replaced with two
  real local interfaces (`TaskWithCategoryRow`, `TaskDeletionMatchRow`) matching each query's
  actual `select()` shape.
- **Verified:** `npm run lint` exits 0.

### ISSUE-002 — `/api/chat` had no error handling past the auth check — **Resolved**
- **Affected files:** `app/api/chat/route.ts`, `components/ChatPanel.tsx`.
- **Fix applied:** The route body (from message validation through building the Sonnet
  stream) is now wrapped in try/catch; any failure (Haiku extraction, Supabase writes,
  malformed JSON body) returns a clean `500` plain-text response instead of an unhandled
  framework error. `ChatPanel.tsx`'s `send()` now checks `res.ok` before treating the
  response as a stream to read, so a failed request shows the intended friendly error message
  instead of rendering raw error text into the chat bubble.
- **Verified:** `npx tsc --noEmit`, `npm run build`, `npm run lint` all pass; not
  independently re-tested against a live forced-failure scenario (e.g. a temporarily invalid
  `ANTHROPIC_API_KEY`) after deploy — logically verified by code review, same confidence
  level as the original finding.

### ISSUE-003 — Life-area grouping feature was unused in production data — **Improved, not fully resolved**
- **Affected files:** `components/Sidebar.tsx`, `lib/groups.ts`.
- **Fix applied:** The group-cycling dot is now larger, bordered, and accompanied by a short
  visible hint line ("Click the dot to tag a category Academic/Personal/Work/Other...") under
  the Categories heading, instead of relying solely on a hover tooltip.
- **Status:** This is a discoverability improvement, not a guarantee of adoption — whether
  the two real users actually start using it is something only real usage over time can
  confirm. Re-check the live `categories.group_name` values in a future session rather than
  assuming this fix "worked."

### ISSUE-004 — Unsplash-hotlinked theme backgrounds — **Resolved**
- **Affected files:** `app/globals.css`, `public/theme/*.jpg` (new), `public/theme/SOURCES.md`
  (new).
- **Fix applied:** All 4 palette background photos are now downloaded and self-hosted at
  `public/theme/{slate,ocean,sunset,forest}.jpg` (1600px wide, quality 55 — chosen for file
  size since they render behind an opacity scrim anyway) and referenced as local `/theme/*.jpg`
  paths instead of hotlinked Unsplash CDN URLs. `public/theme/SOURCES.md` records the original
  Unsplash photo IDs/license for provenance.
- **A new bug was introduced and fixed in the same session:** `proxy.ts`'s auth-gate matcher
  didn't exclude `/theme/*`, so the new local images were being redirected to `/login` for
  logged-out visitors — the exact same class of bug as the earlier `/icon` issue. Fixed by
  broadening the matcher to exclude any path ending in a common static-asset extension
  (`.svg`, `.png`, `.jpg`, `.jpeg`, `.gif`, `.webp`, `.ico`, `.css`, `.js`, `.txt`, `.md`)
  instead of enumerating paths one at a time — see `proxy.ts`'s comment. **If you add a new
  static asset type to `public/` that doesn't end in one of those extensions, add it to this
  list, or it will silently 404-via-redirect for logged-out visitors.**
- **Verified:** Live `curl` checks confirm all 4 images return `200 image/jpeg`; a Playwright
  screenshot of the live `/login` page confirms the background renders correctly.

### ISSUE-005 — Templates could be applied more than once, duplicating tasks — **Resolved**
- **Affected files:** `app/api/templates/route.ts`, `lib/tasks.ts` (new
  `taskExistsWithTitle` helper).
- **Fix applied:** Before inserting each template task, the route now checks whether a task
  with that exact title already exists in that category for that user, and skips it if so.
  Re-applying the same template (or applying it after already doing an example task
  yourself) no longer creates duplicates.

## Remaining known issue (not from the original 5, still open)

### ISSUE-006 — 3 of 4 `npm audit` advisories remain, deliberately unresolved
- **Severity:** Low (build-tooling-related, not runtime-request-path — see `SECURITY.md`)
- **Status:** Open, deliberately deferred
- **Detail:** `npm audit fix` (no `--force`) was run, resolving the `brace-expansion` DoS
  advisory. The other 3 (`postcss`, `sharp`, both transitive via `next`) require
  `npm audit fix --force`, which would bump `next` to `16.3.0` — outside the currently pinned
  `16.2.11`. Not applied, per this repo's "don't upgrade dependencies without deliberate
  review" rule; a framework version bump on a live app with real user data and no test suite
  warrants its own dedicated review pass, not a drive-by fix bundled with unrelated work.
- **Recommended next step:** When there's time to properly test a Next.js upgrade (ideally
  after `TESTING.md`'s test-framework gap is addressed), run `npm audit fix --force`, then
  run the full manual smoke-test checklist in `TESTING.md` before deploying.

---

## DO NOT CHANGE WITHOUT REVIEW

- **`supabase/migrations/*.sql` — never edit a migration that has already been run in
  production.** Add a new numbered migration instead. `004_user_scoping.sql` in particular
  must run in its documented two-pass order (see the comment at the top of that file) —
  running it as one paste will fail because Pass 2's `NOT NULL` constraint requires the
  manual backfill step in between.
- **`lib/supabase.ts`** — the service-role client. Never import it from a `"use client"`
  component (it would leak the service-role key to the browser bundle). It is intentionally
  the *only* client used by `lib/tasks.ts`/`lib/ideas.ts`/`lib/messages.ts` — do not swap it
  for the session-aware client there, since every one of those functions relies on being
  passed `userId` explicitly and filtering manually; RLS is not a substitute unless every call
  site is also re-audited.
- **`proxy.ts`** — the auth gate. Changing its `matcher` config or `PUBLIC_PATHS` list can
  either lock out all users (if a needed public path is removed) or expose protected data (if
  a path is wrongly added to the public list). `/icon`, `/apple-icon`, and any static file
  under `public/` (matched by extension — `.svg|.png|.jpg|.jpeg|.gif|.webp|.ico|.css|.js|.txt|.md`)
  must stay excluded or that asset breaks for logged-out visitors — this exact bug has
  happened twice now (once for `/icon`, once for the new `/theme/*.jpg` background images),
  so if you add a new `public/` asset type with a different extension, add it to the matcher's
  exclusion regex too.
- **`app/api/*/route.ts` auth pattern** — every route must call `requireUserId()` and scope
  every DB call by the returned `userId`. Skipping this on a new route would leak one user's
  data to the other.
- **`.env.local`, and the Vercel dashboard env vars** — never commit real values. Rotating
  `SUPABASE_SERVICE_ROLE_KEY` requires updating both `.env.local` and the Vercel dashboard
  (Production **and** Preview scopes).
- **`categories.name` uniqueness (`unique(user_id, name)`)** combined with the app's
  case-insensitive `.ilike()` lookups — do not "fix" this by relying on the DB constraint
  alone; either keep the case-insensitive check-before-insert pattern in `getOrCreateCategory`
  or add a case-normalizing constraint, but don't remove one without the other.
- **Theme CSS variable names** (`--bg`, `--fg`, `--surface`, `--border`, `--muted`, `--accent`,
  `--accent-fg`, `--bg-art`) in `app/globals.css` — these are consumed by the Tailwind
  `@theme inline` block and referenced by name throughout every component's className strings.
  Renaming one requires updating every usage across `app/`, `components/`.
- **`scripts/create-users.mjs`** — uses the service-role admin API to create real
  authentication accounts. Never run it against production without knowing exactly which
  emails you're adding; there is no corresponding "delete user" script (deletion was done
  manually via an ad hoc script this session, not committed to the repo).
- **Live production data** — 19 tasks / 112 messages / 7 ideas / 3 categories belonging to
  2 real people, at the time of this audit. Never run destructive SQL, a DB reset, or a bulk
  delete against the production Supabase project without explicit user confirmation.

---

## AI working instructions

1. Read `CLAUDE.md` (this file).
2. Read `PROJECT_STATE.md`.
3. Read `TASKS.md`.
4. Read the relevant architecture/feature/API/database docs for the area you're touching.
5. Inspect the affected code before changing it — don't assume this doc is still accurate;
   it was correct as of the audit timestamp printed at the top of this file.
6. Check `git status` before modifying files — don't clobber uncommitted work.
7. Avoid overwriting unrelated work.
8. Make small, reviewable changes.
9. Run relevant verification after changes: at minimum `npx tsc --noEmit` and `npm run build`;
   run `npm run lint` too and note whether your change added *new* errors beyond the 9
   pre-existing ones.
10. Update documentation after meaningful changes (see the permanent rules below).
11. Never claim something works without actually running/verifying it.
12. Never expose secrets (`SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`) in code, logs, or
    chat output.
13. Never modify production data (the live Supabase project — there is no separate
    dev/staging database) without explicit user permission.
14. Never perform destructive database operations (drops, resets, bulk deletes) without
    explicit permission.
15. Never silently replace an existing architectural pattern (e.g. the service-role +
    manual-`userId`-filtering pattern) with a different one without calling it out and getting
    agreement first.
16. Never remove a dependency without checking all usages (`grep -rn` across `app/`,
    `components/`, `lib/`, `scripts/`).
17. Never change authentication, the database schema, deployment configuration, or the RLS
    policies casually — these are exactly the systems listed in
    [DO NOT CHANGE WITHOUT REVIEW](#do-not-change-without-review).
18. Record unresolved uncertainty in the docs rather than guessing — use the same
    Verified/Inferred/Unknown labeling convention established in this audit.
19. Never write a real third-party personal email address, name-linked identifier, or any
    other real PII into any file in this repo (including these `.md` docs) — this repo's
    GitHub remote is public. Refer to the two account holders by role, not by contact info.

### Permanent rules: after every meaningful coding task

1. Update `PROJECT_STATE.md` (current stopping point, what works, what doesn't).
2. Update `TASKS.md` (move/close/add tasks).
3. Append a new entry to `SESSION_LOG.md` — **append, never overwrite** prior entries.
4. Update whichever of `FEATURES.md` / `ARCHITECTURE.md` / `API_REFERENCE.md` /
   `DATABASE.md` / `TESTING.md` / `DEPLOYMENT.md` / `SECURITY.md` is affected by the change.
5. Remove or correct any documentation that your change made stale.
6. Record meaningful architectural decisions in `DECISIONS.md`.
7. Run relevant verification (`npx tsc --noEmit`, `npm run build`, `npm run lint`).
8. Clearly record anything you could not verify.
9. Treat this repository's `.md` files as the permanent memory system — a future session
   (possibly on a different account, with zero chat history) must be able to resume from them
   alone.
